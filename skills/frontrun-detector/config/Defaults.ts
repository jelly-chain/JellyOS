// Defaults - Configuration defaults
// Author: Tentacle OS

export const defaultTimeouts = {
  request: 30000,
  websocket: 60000,
};

export const defaultThresholds = {
  minAmount: 1000,
  maxSlippage: 0.01,
};
